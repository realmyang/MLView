{
  "name": "mlview-local",
  "owner": { "name": "MLView" },
  "metadata": {
    "description": "MLView's marketplace: the plugin from this checkout, and the same plugin fetched from GitHub.",
    "version": "0.1.0"
  },
  "plugins": [
    {
      "name": "mlview",
      "source": "./claude-plugin",
      "description": "Visualize and audit Python ML pipelines as an interactive diagram — fully local, static analysis only."
    },
    {
      "name": "mlview-github",
      "source": {
        "source": "github",
        "repo": "realmyang/MLView"
      },
      "description": "The same MLView plugin, installed from GitHub — for a machine with no MLView checkout. The analyzer is vendored inside the plugin, so there is nothing to pip install."
    }
  ]
}
