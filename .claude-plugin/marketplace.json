{
  "name": "mlview-local",
  "owner": { "name": "MLView" },
  "metadata": {
    "description": "MLView's marketplace: the plugin from this checkout, and the same plugin fetched from GitHub.",
    "version": "0.1.0"
  },
  "plugins": [
    {
      "name": "mlview",
      "source": "./claude-plugin",
      "description": "Visualize and audit Python ML pipelines as an interactive diagram — fully local, static analysis only."
    },
    {
      "name": "mlview-github",
      "source": {
        "source": "git-subdir",
        "url": "https://github.com/realmyang/MLView.git",
        "path": "claude-plugin"
      },
      "description": "The same MLView plugin, installed from GitHub — for a machine with no MLView checkout. The plugin lives in the repository's claude-plugin/ subdirectory, so this is the git-subdir source; the analyzer is vendored inside it, so there is nothing to pip install."
    }
  ]
}
