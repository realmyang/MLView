defaults:
  - _self_
  - data: cifar_small
  - model: resnet_tiny
  - optimizer: adamw

seed: 1234
epochs: 40
device: cuda
out_dir: outputs/${now:%Y-%m-%d}/${now:%H-%M-%S}

hydra:
  run:
    dir: ${out_dir}
  job:
    chdir: true
