{
  "name": "hf_no_eval",
  "title": "HuggingFace fine-tune that never evaluates anything",
  "origin": "written alongside ANA-7 to exercise MLV708 (Sprint 4)",
  "tuned": true,
  "tunedWhy": "the rules were developed against this project, so its numbers are a ceiling, not a measurement - it is excluded from the unseen headline in ACCURACY.md",
  "labelledBy": "ANA-7, 2026-09-09",
  "note": "No `graph` block: added for its findings, not for a hand-drawn diagram.",
  "expected": [
    {"code": "MLV708", "file": "train.py", "line": 45, "symbol": "Trainer(...)",
     "severity": "medium", "verdict": "expected",
     "defect": "the Trainer has no eval_dataset, no compute_metrics, and TrainingArguments names no evaluation strategy",
     "why": "the only number the run reports is the training loss, so overfitting is invisible and the checkpoint kept is the last one rather than the best one"}
  ],
  "forbidden": [
    {"code": "MLV601", "file": "train.py",
     "why": "transformers.set_seed(SEED) is called at line 22 and TrainingArguments carries seed=SEED"},
    {"code": "MLV201", "file": "train.py",
     "why": "the HuggingFace Trainer owns the optimisation loop - no hand-written backward/step exists here"},
    {"code": "MLV202", "file": "train.py",
     "why": "same: there is no gradient update in this file for a rule to find missing"},
    {"code": "MLV305", "file": "train.py",
     "why": "no metric is computed anywhere in this project - that absence is what MLV708 reports, and MLV305 has nothing to judge"}
  ]
}
