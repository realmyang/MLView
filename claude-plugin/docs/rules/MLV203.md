# MLV203 - optimizer.step() runs before loss.backward()

| | |
|---|---|
| **Severity** | `high` - red octagon - very likely a real defect that silently corrupts results or crashes |
| **Frameworks** | torch |
| **Base prior** | 0.95 |
| **Rule version** | 1 |
| **Tags** | `correctness`, `train-loop` |
| **Absence rule** | no |
| **Enabled** | yes |

## Why it matters

The step applies whatever gradients the previous iteration left behind, so every update is one batch stale and the first one is pure noise.

## How it is detected

Within one block of a batch loop, an `OPT_STEP` whose statement index is lower than the `backward()`'s - or the variant where a `zero_grad` sits between the backward and the step and wipes the gradients that were just computed.

## False positives it avoids

- Multi-optimizer GAN loops, where `d_optimizer.step()` legitimately precedes `g_loss.backward()`: the rule requires exactly one optimizer and one loss in the loop, so the two are linked.
- Calls in different blocks, which are not on one iteration path.

## How to fix it

Order the training step as zero_grad() -> forward -> loss.backward() -> (clip) -> optimizer.step().

## Example that fires

`analyzer/tests/fixtures/rules/MLV203_bad.py`

```python
"""The step runs before the backward, so it applies last iteration's gradients."""
import torch
import torch.nn as nn
import torch.optim as optim
from torch.utils.data import DataLoader, TensorDataset


def train(dataset: TensorDataset, epochs: int = 3) -> nn.Module:
    torch.manual_seed(0)
    model = nn.Sequential(nn.Linear(10, 3))
    criterion = nn.CrossEntropyLoss()
    optimizer = optim.SGD(model.parameters(), lr=0.01)
    train_loader = DataLoader(dataset, batch_size=32, shuffle=True)

    model.train()
    for epoch in range(epochs):
        for features, labels in train_loader:
            loss = criterion(model(features), labels)
            optimizer.step()
            loss.backward()
            optimizer.zero_grad()
    return model
```

## Example that does not

`analyzer/tests/fixtures/rules/MLV203_good.py` - the nearest false-positive trap this rule has to survive.

```python
"""The trap: a GAN step where d_optimizer.step() legitimately runs before
g_loss.backward(). Two optimizers and two losses, so the ordering carries no
information and the rule must stay quiet."""
import torch
import torch.nn as nn
import torch.optim as optim
from torch.utils.data import DataLoader, TensorDataset


def train(dataset: TensorDataset, epochs: int = 3) -> None:
    torch.manual_seed(0)
    generator = nn.Sequential(nn.Linear(10, 10))
    discriminator = nn.Sequential(nn.Linear(10, 1))
    criterion = nn.BCEWithLogitsLoss()
    g_optimizer = optim.Adam(generator.parameters(), lr=2e-4)
    d_optimizer = optim.Adam(discriminator.parameters(), lr=2e-4)
    train_loader = DataLoader(dataset, batch_size=32, shuffle=True)

    for epoch in range(epochs):
        for real, labels in train_loader:
            d_optimizer.zero_grad()
            d_loss = criterion(discriminator(real), labels)
            d_loss.backward()
            d_optimizer.step()

            g_optimizer.zero_grad()
            fake = generator(real)
            g_loss = criterion(discriminator(fake), labels)
            g_loss.backward()
            g_optimizer.step()
```

## Suppressing it

```python
result = risky_call()  # mlview: ignore[MLV203]
```

Or `# mlview: ignore-file` in the first five lines of the file, or
`.mlview.toml`:

```toml
[rules]
disable = ["MLV203"]
```

A suppressed issue is still emitted with `suppressed: true`, so the viewer can offer "show suppressed"; hosts never publish it as an editor diagnostic.

---

*Generated by `analyzer/tools/gen_rule_docs.py` from the rule registry and the fixtures - do not edit by hand.*
