# Follow source evidence

Select a node, edge, or finding in a generated diagram and open its evidence.
Notebook citations identify a cell and lines within it. Conceptual groups can
have no jump target. Stale source references are blocked instead of jumping to
an unrelated statement at an old line number.

Alt+M remains a legacy static report action. Authored diagram navigation starts
from the citations the assistant supplied.
