# Sources for `RESEARCH_COVERAGE_ACCURACY.md`

Every claim in `docs/RESEARCH_COVERAGE_ACCURACY.md` resolves to a numbered entry
here or to a local measurement whose command is printed in that document's
section 2. One line per source: what it is, and what it is used for.

**Verification marks.** Sources are marked with how far they were checked:

| mark | meaning |
|------|---------|
| `[v]` | fetched and quoted **in this session**; the exact sentence relied on was read |
| `[r]` | fetched during the research pass that fed this document; not re-fetched here |
| `[!]` | could not be fully verified — the limit is stated on the entry, and no claim rests on more than the entry supports |

Of the 120 entries, 100 carry a specific inline claim in the document; the
remaining 20 are background that was read to bound a scope decision or to
confirm that a candidate had no better source, and no claim rests on them.

Nothing in this file is a reconstructed citation. Where a source could not be
retrieved, the entry says so and the corresponding claim in the document is
either dropped or attributed to local measurement instead.

---

## A. PyTorch — primary documentation and issue tracker

| # | mark | Source | Used for |
|---|------|--------|----------|
| 1 | `[v]` | [torch.load — PyTorch 2.14](https://docs.pytorch.org/docs/2.14/generated/torch.load.html) | Signature shows `weights_only=True` as the current default; `map_location` is documented as device remapping with no correctness warning. Grounds §6.5 (MLV803's message is inverted against modern PyTorch). |
| 2 | `[v]` | [torch.utils.data — PyTorch 2.14](https://docs.pytorch.org/docs/2.14/data.html) | Verbatim: "In distributed mode, calling the `set_epoch()` method at the beginning of each epoch **before** creating the `DataLoader` iterator is necessary to make shuffling work properly across multiple epochs." And: "seeds for other libraries may be duplicated upon initializing workers, causing each worker to return identical random numbers." Grounds **C-09** and **C-26**. |
| 3 | `[r]` | [Reproducibility — PyTorch 2.14 notes](https://docs.pytorch.org/docs/2.14/notes/randomness.html) | The complete seeding checklist (`torch.manual_seed`, `random.seed`, `np.random.seed`, `worker_init_fn` + `generator`, `use_deterministic_algorithms(True)`, `cudnn.benchmark=False`). Grounds **C-07**, **C-26** and **C-34**. Note: this page does **not** mention `CUBLAS_WORKSPACE_CONFIG`; see [16]. |
| 4 | `[r]` | [torch.amp — PyTorch 2.14](https://docs.pytorch.org/docs/2.14/amp.html) | "Gradient values with small magnitudes may not be representable in float16. These values will flush to zero (\"underflow\"), so the update for the corresponding parameters will be lost." Also: "Autocast should wrap only the forward pass(es)… Backward passes under autocast are not recommended." Grounds **C-10** (all four AMP sub-variants). |
| 5 | `[r]` | [CUDA Automatic Mixed Precision examples — PyTorch 2.14](https://docs.pytorch.org/docs/2.14/notes/amp_examples.html) | The normative `scale → backward → unscale_ → clip → step → update` order, and "`unscale_` should only be called once per optimizer per `step` call." Grounds **C-10** (the clip-without-unscale arm). |
| 6 | `[r]` | [torch.nn.functional.dropout — PyTorch 2.14](https://docs.pytorch.org/docs/2.14/generated/torch.nn.functional.dropout.html) | Signature `dropout(input, p=0.5, training=True, inplace=False)` — `training` defaults to **True**, so a functional dropout inside a Module with no `training=` stays on under `model.eval()`. Grounds **C-03**. |
| 7 | `[r]` | [torch.nn.Module — PyTorch 2.14](https://docs.pytorch.org/docs/2.14/generated/torch.nn.Module.html) | "one should call the Module instance afterwards instead of this since the former takes care of running the registered hooks while the latter silently ignores them" (on `forward`); and that `train()`/`eval()` affect Dropout and BatchNorm only. Grounds **C-33** (direct `.forward()`) and **C-29** (`train()` never restored). |
| 8 | `[r]` | [torch.nn.CTCLoss — PyTorch](https://docs.pytorch.org/docs/2.9/generated/torch.nn.modules.loss.CTCLoss.html) | "requires logarithmized probabilities of the outputs, obtained with `torch.nn.functional.log_softmax()`." Grounds **C-14**. |
| 9 | `[r]` | [torch.nn.CrossEntropyLoss — PyTorch 2.14](https://docs.pytorch.org/docs/2.14/generated/torch.nn.CrossEntropyLoss.html) | Expects "unnormalized logits"; `ignore_index` defaults to `-100`. Grounds `MLV401`'s premise and the **C-14** generalisation. |
| 10 | `[r]` | [torch.optim — PyTorch 2.14](https://docs.pytorch.org/docs/2.14/optim.html) | The per-parameter-group example that excludes bias terms from weight decay; and the 1.1.0 BC-breaking scheduler-ordering note. Grounds §9.4 and §9.6. Contains **no** warning that the model must be moved to GPU before the optimizer is built — the evidence for rejecting that folk rule (§9.3). |
| 11 | `[r]` | [torch.optim — PyTorch 0.4.0 (historical)](https://docs.pytorch.org/docs/0.4.0/optim.html) | The retired sentence "If you need to move a model to GPU via `.cuda()`, please do so before constructing optimizers for it." The origin of the folk rule rejected in §9.3. |
| 12 | `[r]` | [torch.onnx — PyTorch 2.14](https://docs.pytorch.org/docs/2.14/onnx.html) | "By default the exported model will have the shapes of all input and output tensors set to exactly match those given in `args`"; "The tracer records the shapes of the example inputs"; `dynamic_shapes` is preferred when `dynamo=True`. Grounds **C-08** and **C-32**. |
| 13 | `[r]` | [Export a PyTorch model to ONNX (tutorial)](https://docs.pytorch.org/tutorials/beginner/onnx/export_simple_model_to_onnx_tutorial.html) | The exporter's own warning: "Exporting a model while it is in training mode… Calling `model.eval()` before export is recommended." First-party confirmation that **C-08** detects a defect. |
| 14 | `[r]` | [PyTorch Performance Tuning Guide (recipe)](https://docs.pytorch.org/tutorials/recipes/recipes/tuning_guide.html) | `num_workers>0` + `pin_memory=True`; "Avoid unnecessary CPU-GPU synchronization" naming `.item()`, `.cpu()`, `print(cuda_tensor)`; `zero_grad(set_to_none=True)`; channels-last; `cudnn.benchmark`. Grounds the performance advisory group (**C-36**) and the rejections in §9.5. |
| 15 | `[r]` | [Multi-GPU training with DDP (tutorial)](https://docs.pytorch.org/tutorials/beginner/ddp_series_multigpu.html) | Shows the canonical `self.train_data.sampler.set_epoch(epoch)` at the top of each epoch — the positive fixture shape for **C-09**. |
| 16 | `[r]` | [pytorch/pytorch#41377 — deterministic algorithms and CUBLAS_WORKSPACE_CONFIG](https://github.com/pytorch/pytorch/pull/41377) | PyTorch raises when `use_deterministic_algorithms(True)` is set on CUDA ≥ 10.2 without `CUBLAS_WORKSPACE_CONFIG`, which must be set before the CUDA context exists. Grounds the tier-3 determinism-environment candidate and its honest limitation (shell-set env vars are invisible). |
| 17 | `[r]` | [pytorch/pytorch#7844 — "parameters after .cuda() will be different objects" is wrong](https://github.com/pytorch/pytorch/issues/7844) | `Module._apply` mutates `param.data` in place, so parameter identity survives `.to(device)`. The refutation behind §9.3. |
| 18 | `[r]` | [pytorch/pytorch#4534 — BatchNorm1d fails with batch size 1](https://github.com/pytorch/pytorch/issues/4534) | Origin of "Expected more than 1 value per channel when training". Grounds the BatchNorm/batch-size-1 candidate (tier 3). |
| 19 | `[r]` | [PyTorch forum — softmax/log_softmax in CTC loss](https://discuss.pytorch.org/t/softmax-log-softmax-in-ctc-loss/109311) | Practitioner confirmation that plain softmax into `CTCLoss` trains to all-blank rather than raising — i.e. the **C-14** defect is silent. |

## B. scikit-learn and imbalanced-learn

| # | mark | Source | Used for |
|---|------|--------|----------|
| 20 | `[r]` | [Common pitfalls and recommended practices — scikit-learn](https://scikit-learn.org/stable/common_pitfalls.html) | §12.1 inconsistent preprocessing (fit on train, forget to transform test — MSE 62.80 vs 0.90 in their example); §12.2 "never call `fit` on the test data", with `SelectKBest(k=25).fit_transform(X, y)` before the split as the worked leak; §12.3 integer vs `RandomState` instance for CV splitters. Grounds `MLV101`/`MLV102`/`MLV103`, **C-25**, **C-23** and §6.5. |
| 21 | `[r]` | [Cross-validation — scikit-learn User Guide](https://scikit-learn.org/stable/modules/cross_validation.html) | "`cross_val_predict` is not an appropriate measure of generalization error"; the time-ordered-articles warning; `GroupKFold` guarantees "the same group is not represented in both testing and training sets". Grounds **C-20** and the tier-3 `cross_val_predict` candidate. |
| 22 | `[v]` | [GroupShuffleSplit — scikit-learn API](https://scikit-learn.org/stable/modules/generated/sklearn.model_selection.GroupShuffleSplit.html) | Signature `GroupShuffleSplit(n_splits=5, *, test_size=None, train_size=None, random_state=None)` — **no `shuffle` parameter exists**. Direct evidence for the `MLV106` splitter-table defect **D2** (§6.5). |
| 23 | `[v]` | [StratifiedGroupKFold — scikit-learn API](https://scikit-learn.org/stable/modules/generated/sklearn.model_selection.StratifiedGroupKFold.html) | Signature `StratifiedGroupKFold(n_splits=5, shuffle=False, random_state=None)` — `shuffle` defaults to **False**. The other half of the same defect. |
| 24 | `[r]` | [TimeSeriesSplit — scikit-learn API](https://scikit-learn.org/stable/modules/generated/sklearn.model_selection.TimeSeriesSplit.html) | `gap` is "Number of samples to exclude from the end of each train set before the test set". Grounds **C-22**(g), the horizon/`gap=` candidate. |
| 25 | `[r]` | [Encoding categorical features / TargetEncoder — scikit-learn](https://scikit-learn.org/stable/modules/preprocessing.html#target-encoder) | "In `TargetEncoder`, `fit(X, y).transform(X)` does not equal `fit_transform(X, y)`"; "It is discouraged to use this method [`fit`] because it can introduce data leakage." Grounds **C-21**'s second arm. |
| 26 | `[r]` | [Pipelines and composite estimators — scikit-learn](https://scikit-learn.org/stable/modules/compose.html) | "Pipelines help avoid leaking statistics from your test data into the trained model in cross-validation." The fix-hint wording for MLV103 and the leakage candidates. |
| 27 | `[r]` | [Probability calibration — scikit-learn](https://scikit-learn.org/stable/modules/calibration.html) | "Using the classifier output of training data to fit the calibrator would thus result in a biased calibrator." Grounds the calibration sibling recorded under **C-30**. |
| 28 | `[r]` | [Tuning the decision threshold — scikit-learn](https://scikit-learn.org/stable/modules/classification_threshold.html) | "You should never use the same data for training the classifier and tuning the decision threshold due to the risk of overfitting." Grounds **C-30**. |
| 29 | `[r]` | [Metrics and scoring — scikit-learn](https://scikit-learn.org/stable/modules/model_evaluation.html) | `balanced_accuracy` "avoids inflated performance estimates on imbalanced datasets"; micro-averaged P/R/F equal accuracy when all labels are included. Grounds the `MLV307` sibling under **C-30**, and §9.7. |
| 30 | `[r]` | [roc_auc_score — scikit-learn API](https://scikit-learn.org/stable/modules/generated/sklearn.metrics.roc_auc_score.html) | `y_score` must be "probability estimates… `predict_proba(X)[:, 1]`" or "the output of `decision_function`" — never `predict()`. The normative basis for `MLV306` and for the framework-aware producer test in §6.5 (**D1**). |
| 31 | `[r]` | [r2_score — scikit-learn API](https://scikit-learn.org/stable/modules/generated/sklearn.metrics.r2_score.html) | "a constant model that always predicts the average y… would get a R² score of 0.0" — the in-sample-mean baseline. Grounds the forecasting-metric sibling under **C-30**. |
| 32 | `[r]` | [Common pitfalls — imbalanced-learn](https://imbalanced-learn.org/stable/common_pitfalls.html) | Warns against resampling "the entire dataset before splitting it into a train and a test partitions", and that `imblearn.pipeline.Pipeline` "avoids to make any data leakage". Grounds **C-01**. |

## C. Keras and TensorFlow

| # | mark | Source | Used for |
|---|------|--------|----------|
| 33 | `[v]` | [Model training APIs — Keras 3](https://keras.io/api/models/model_training_apis/) | Verbatim: "The validation data is selected from the last samples in the `x` and `y` data provided, before shuffling."; `shuffle` "is ignored when `x` is a `keras.utils.PyDataset`, `tf.data.Dataset`, `torch.utils.data.DataLoader` or Python generator function."; "When passing an infinitely repeating dataset, you must specify the `steps_per_epoch` argument, otherwise the training will run indefinitely." Grounds **C-15**, **C-16** and **C-18**. |
| 34 | `[r]` | [EarlyStopping — Keras 3 callbacks](https://keras.io/api/callbacks/early_stopping/) | `monitor="val_loss"` and `restore_best_weights=False` are the defaults; "If False, the model weights obtained at the last step of training are used." Grounds **C-19** and bounds §9.8. |
| 35 | `[r]` | [Probabilistic losses — Keras 3](https://keras.io/api/losses/probabilistic_losses/) | `from_logits` defaults to False on the crossentropy family; logits-plus-`from_logits=True` is the recommended pairing. Grounds `MLV709` and **C-17**. |
| 36 | `[r]` | [Accuracy metrics — Keras 3](https://keras.io/api/metrics/accuracy_metrics/) | `BinaryAccuracy` thresholds at 0.5; `CategoricalAccuracy`/`SparseCategoricalAccuracy` state "You can provide logits of classes as `y_pred`, since argmax of logits and probabilities are same." The fact that splits **C-17** into a defect (binary) and a non-defect (categorical). |
| 37 | `[r]` | [keras/src/trainers/compile_utils.py](https://github.com/keras-team/keras/blob/master/keras/src/trainers/compile_utils.py) | The string `"accuracy"` resolves by **shape**, not by loss: `is_binary = y_p_last_dim == 1`. Makes **C-17**'s binary arm decidable from a literal `Dense(1)` head. |
| 38 | `[r]` | [BatchNormalization — Keras 3](https://keras.io/api/layers/normalization_layers/batch_normalization/) | `training=True` uses batch statistics; "setting `trainable = False` on the layer means that the layer will be subsequently run in inference mode"; changes to `trainable` after `compile()` require recompiling. Grounds the tier-3 Keras fine-tuning candidate. |
| 39 | `[r]` | [Transfer learning & fine-tuning — Keras guides](https://keras.io/guides/transfer_learning/) | "if you change any `trainable` value, make sure to call `compile()` again on your model for your changes to be taken into account." Same candidate. |
| 40 | `[r]` | [TextVectorization — Keras 3](https://keras.io/api/layers/preprocessing_layers/text/text_vectorization/) | `adapt()` is the Keras spelling of `fit` and is documented as a training-split operation. Grounds the `adapt` knowledge rows in §6.2 (batch 4). |
| 41 | `[r]` | [tf.data: Build TensorFlow input pipelines](https://www.tensorflow.org/guide/data) | "a shuffle placed before a repeat will show every element of one epoch before moving to the next"; "a repeat before a shuffle mixes the epoch boundaries together"; the canonical `shuffle().batch().prefetch()` chain. Grounds **C-11** and the tier-2 repeat/shuffle candidate. This guide states **no blanket prohibition** on shuffle-after-batch; C-04's message reasons from `batch()` semantics and says so. |
| 42 | `[!]` | [Better performance with the tf.data API](https://www.tensorflow.org/guide/data_performance) | Recommends caching after a costly `map` and notes that buffering transformations hold internal buffers. **Limit:** does not state the "cache freezes the permutation" consequence; **C-12** argues that from `cache()` semantics and the rule text must do the same. |
| 43 | `[r]` | [Working with preprocessing layers — TensorFlow](https://www.tensorflow.org/guide/keras/preprocessing_layers) | `Normalization`/`StringLookup`/`IntegerLookup` `.adapt` semantics and the `tf.data` `.map` idiom. Same use as [40] — §6.2 batch 4. |

## D. HuggingFace and Lightning

| # | mark | Source | Used for |
|---|------|--------|----------|
| 44 | `[r]` | [Trainer / TrainingArguments — Transformers](https://huggingface.co/docs/transformers/en/main_classes/trainer) | `load_best_model_at_end` "Requires `eval_strategy` to be set. When True, `save_strategy` must match `eval_strategy` (unless `save_strategy` is 'best')"; `eval_strategy` defaults to `"no"`; `data_seed`; `full_determinism`. Grounds `MLV708`, §9.9, **C-02**, and the `save_strategy='best'` exemption. |
| 45 | `[r]` | [Padding and truncation — Transformers](https://huggingface.co/docs/transformers/main/en/pad_truncation) | "False or `do_not_truncate`: no truncation is applied. This is the default behavior." Grounds the tier-2 tokenizer candidate. |
| 46 | `[r]` | [Text generation — Transformers](https://huggingface.co/docs/transformers/en/llm_tutorial) | Demonstrates that right-padded batched generation on a decoder-only LM produces garbage and that `padding_side='left'` fixes it. Grounds the tier-3 generation candidate. |
| 47 | `[r]` | [Data collator — Transformers](https://huggingface.co/docs/transformers/en/main_classes/data_collator) | `DataCollatorForLanguageModeling` has `mlm=True` by default; `label_pad_token_id` defaults to `-100`. Grounds the tier-2 causal-LM collator candidate. |
| 48 | `[r]` | [Token classification task guide — Transformers](https://huggingface.co/docs/transformers/en/tasks/token_classification) | The `-100` sub-token alignment convention. Reference shape for the same. |
| 49 | `[r]` | [Process — HuggingFace Datasets](https://huggingface.co/docs/datasets/process) | `Dataset.shuffle` and `Dataset.train_test_split` both take `seed`/`generator` and default to a fresh RNG. Grounds the MLV602 extension in §6.5. |
| 50 | `[r]` | [Gradient accumulation — Accelerate](https://huggingface.co/docs/accelerate/en/usage_guides/gradient_accumulation) | The reference loop divides the loss by `gradient_accumulation_steps`; `accelerator.accumulate()` "will automatically know… how to adjust the loss". Grounds the accumulation candidate and its framework exemption. |
| 51 | `[r]` | [Manual Optimization — PyTorch Lightning (docs source)](https://github.com/Lightning-AI/pytorch-lightning/blob/master/docs/source-pytorch/model/manual_optimization.rst) | Under manual optimization the Trainer's `gradient_clip_val` and `accumulate_grad_batches` no longer apply; `configure_gradient_clipping()` is not invoked. Grounds the tier-2 Lightning candidate. |
| 52 | `[r]` | [LightningModule guide — PyTorch Lightning (docs source)](https://github.com/Lightning-AI/pytorch-lightning/blob/master/docs/source-pytorch/common/lightning_module.rst) | Two "don't do in Lightning" blocks: manual `x.to(device)` inside a LightningModule, and hand-built `DistributedSampler`. Grounds the **C-09** framework exemption and a tier-3 candidate. |
| 53 | `[r]` | [LightningDataModule — PyTorch Lightning (docs source)](https://github.com/Lightning-AI/pytorch-lightning/blob/master/docs/source-pytorch/data/datamodule.rst) | "It is not recommended to assign state here… since it is called on a single process" (on `prepare_data`). Grounds the tier-3 datamodule candidate. |
| 54 | `[r]` | [lightning/fabric/utilities/seed.py — seed_everything](https://lightning.ai/docs/fabric/stable/_modules/lightning/fabric/utilities/seed.html) | `seed_everything(seed, workers=True)` installs `pl_worker_init_function`; a user-supplied `worker_init_fn` wins. The exact negation **C-26** must honour. |
| 55 | `[r]` | [Lightning-AI/pytorch-lightning#9330 — clip before AMP unscale](https://github.com/Lightning-AI/pytorch-lightning/issues/9330) | Independent evidence that clipping still-scaled gradients is a real, silent defect — serious enough to be a framework bug. Grounds **C-10**'s clip arm. |

## E. pandas, gradient boosting, and other libraries

| # | mark | Source | Used for |
|---|------|--------|----------|
| 56 | `[r]` | [Copy-on-Write — pandas User Guide](https://pandas.pydata.org/docs/user_guide/copy_on_write.html) | "chained assignment will never work"; `df["foo"].replace(1, 5, inplace=True)` "Does NOT update df anymore". Grounds the pandas candidates noted in §4.7 and the IR prerequisite in §6.1 (IR-7). |
| 57 | `[r]` | [DataFrame.rolling — pandas API](https://pandas.pydata.org/docs/reference/api/pandas.DataFrame.rolling.html) | `closed` defaults to `'right'`, i.e. `(first, last]` — "the last point is included in the calculations"; `center=True` labels the window at its centre. Grounds **C-22** and its `center` variant. |
| 58 | `[r]` | [DataFrame.bfill — pandas API](https://pandas.pydata.org/docs/reference/api/pandas.DataFrame.bfill.html) | bfill propagates "non-null values from later positions to earlier positions". Grounds **C-23**. |
| 59 | `[r]` | [XGBoost Python package introduction](https://xgboost.readthedocs.io/en/stable/python/python_intro.html) | "Early stopping requires at least one set in `evals`. If there's more than one, it will use the last."; `xgboost.train()` "will return a model from the last iteration, not the best one." Grounds **C-02**. Notably the docs issue **no** warning against pointing `eval_set` at the test split. |
| 60 | `[r]` | [xgboost.Booster.predict — XGBoost API](https://xgboost.readthedocs.io/en/stable/python/python_api.html) | Native `Booster.predict` returns probabilities for `binary:logistic` (raw margin only with `output_margin=True`). Confirms the already-shipped PUB2-08 knowledge fix and bounds §6.5 (**D1**). |
| 61 | `[r]` | [lightgbm.early_stopping — LightGBM API](https://lightgbm.readthedocs.io/en/stable/pythonapi/lightgbm.early_stopping.html) | "Requires at least one validation data and one metric." Same use as [59] — **C-02**. |
| 62 | `[r]` | [Transforming categorical features to numerical — CatBoost](https://catboost.ai/docs/en/concepts/algorithm-main-stages_cat-to-numberic) | Ordered target statistics are "calculated individually for each object using data from previous objects" — the library avoids the naive global target mean on purpose. The counter-example that makes a hand-rolled `groupby(col)[target].transform('mean')` a defect (**C-21**). |
| 63 | `[r]` | [Data — MONAI](https://docs.monai.io/en/stable/data.html) | `monai.data.DataLoader` is a `torch.utils.data.DataLoader` subclass and `CacheDataset` a Dataset — so the knowledge rows are mechanical. Grounds the MONAI table in §6.2 (batch 2). |
| 64 | `[r]` | [timm/data/transforms_factory.py](https://github.com/huggingface/pytorch-image-models/blob/main/timm/data/transforms_factory.py) | `is_training=True` selects RandomResizedCrop + auto-augment + random erasing; `False` selects Resize + CenterCrop. One literal keyword decides train-vs-eval. Grounds the `MLV114` extension in §6.5 (D4) and §6.2 (batch 4). |
| 65 | `[r]` | [Diagnostics — Prophet](https://facebook.github.io/prophet/docs/diagnostics.html) | `cross_validation` fits "using data only up to that cutoff point". The correct-usage baseline for the tier-3 forecasting candidate. |
| 66 | `[r]` | [ARIMAResults.predict — statsmodels](https://www.statsmodels.org/stable/generated/statsmodels.tsa.arima.model.ARIMAResults.predict.html) | With the default `dynamic=False`, "Prior to this observation, true endogenous values will be used for prediction" — in-sample `predict()` is a one-step-ahead fit, not a forecast. Same candidate. |
| 67 | `[r]` | [os.listdir — Python standard library](https://docs.python.org/3/library/os.html#os.listdir) | "The list is in arbitrary order." The normative basis for **C-27** (a class-index mapping that changes between machines). |
| 68 | `[r]` | [PEP 618 — Add optional length-checking to zip](https://peps.python.org/pep-0618/) | "the default behavior of `zip` means that faulty refactoring or logic errors could easily result in silently losing data"; `strict=True` added in 3.10. Grounds the tier-3 two-loader `zip` candidate (§9.13's sibling). |

## F. Tool rule catalogs (prior art, differenced against MLView's catalog)

| # | mark | Source | Used for |
|---|------|--------|----------|
| 69 | `[r]` | [TorchFix — a linter for PyTorch code](https://github.com/meta-pytorch/torchfix) | 15 codes: TOR001–004, TOR101–108, TOR201–203, TOR401–402, TOR901. Includes TOR002 (`require_grad` typo — **C-05**), TOR003 (`use_reentrant`), TOR102 (`torch.load`). Also the **TOR4XX precedent**: noisy rules ship disabled by default, which is the model for MLView's advisory group (§6.7). |
| 70 | `[r]` | [torchfix/visitors — source](https://github.com/meta-pytorch/torchfix/tree/main/torchfix/visitors) | The codes the README omits: TOR106 `log1p`, TOR107 `expm1`, TOR108 `logsumexp`, TOR201 `pretrained=`, TOR202 `v2.ToTensor`, TOR401 `num_workers=0`, TOR402 `zero_grad` without `set_to_none`. Message text quoted from here rather than paraphrased. |
| 71 | `[r]` | [SonarSource/sonar-python — rule metadata](https://github.com/SonarSource/sonar-python/tree/master/python-checks/src/main/resources/org/sonar/l10n/py/rules/python) | 450 Python rules, of which 31 carry an ML/data-science tag: S6969/6971/6972/6973/6974 (sklearn), S6978/6979/6982/6983/6985 (PyTorch), S6908/6911/6918/6919/6925/6928/6929 (TensorFlow), S6709/6711/6714/6725/6729/6730 (NumPy), S6734/6735/6740/6741/6742/6894 (pandas). The backbone of the coverage matrix in §4. |
| 72 | `[r]` | [Sonar rule S6982 (HTML description)](https://github.com/SonarSource/sonar-python/blob/master/python-checks/src/main/resources/org/sonar/l10n/py/rules/python/S6982.html) | "raises an issue when a PyTorch model state is loaded and `torch.nn.Module.eval()` or `.train()` is not called". Cited in §9.1 as the rule MLView must **not** adopt as stated (measured 339 firings on the pinned corpus). |
| 73 | `[r]` | [Write Clean Machine Learning code with PyTorch — Sonar Community](https://community.sonarsource.com/t/write-clean-machine-learning-code-with-pytorch/126833) | The newer PyTorch rules S6984, S7616, S7675, S7695 that are not in the metadata snapshot. **Limit:** the page carries no publication year; cited as undated. |
| 74 | `[r]` | [ruff — crates/ruff_linter/src/codes.rs](https://github.com/astral-sh/ruff/blob/main/crates/ruff_linter/src/codes.rs) | The complete PD (13) and NPY (4) rule sets. Establishes that ruff's ML surface is style and deprecation only — no semantic ML rule. §4. |
| 75 | `[r]` | [dslinter — ML code-smell checklist (SERG-Delft)](https://hynn01.github.io/dslinter/posts/codesmells/) | The 22-smell catalogue: data leakage, randomness uncontrolled, deterministic option not used, gradients not cleared, train/eval mode toggling, PyTorch call-method misused, threshold-dependent validation, chain indexing, memory not freed, no scaling before a scaling-sensitive operation. The closest prior art to MLView's catalog; used throughout §4 and §5. |
| 76 | `[r]` | [SERG-Delft/dslinter — tool](https://github.com/SERG-Delft/dslinter) | The pylint plugin implementing [75]. |
| 77 | `[r]` | [UMLAUT heuristics.py (BerkeleyHCI)](https://raw.githubusercontent.com/BerkeleyHCI/umlaut/master/umlaut/heuristics.py) | The tool's verbatim check list: softmax-before-loss, missing activations, dropout `p > 0.5`, input normalization, NaN loss, learning rate outside `[1e-7, 1e-2]`. Read in preference to the paper because it is the authoritative rule list. Grounds the hyperparameter-range candidate held in §9. |
| 78 | `[r]` | [Pynblint (collab-uniba)](https://github.com/collab-uniba/pynblint) | 22 notebook-hygiene rules (re-executability, imports at top, relative paths, modularization). Establishes that the notebook tooling layer is hygiene, not ML semantics. **Limit:** the README states "22 linting rules" without enumerating identifiers, so only rule *families* are described in §4. |
| 79 | `[r]` | [nbQA](https://github.com/nbQA-dev/nbQA) | A delegating runner with no rules of its own. Confirms there is no notebook-native ML rule catalog to merge. |
| 80 | `[r]` | [Deepchecks — tabular data-integrity checks](https://docs.deepchecks.com/stable/tabular/auto_checks/data_integrity/index.html) | ~15 data-integrity checks plus a train/test-validation category, all requiring the data at runtime. The scope boundary argued in §9.10. |
| 81 | `[r]` | [Great Expectations — validate data](https://docs.greatexpectations.io/docs/0.18/oss/guides/validation/validate_data_overview/) | 300+ expectations, all evaluated against real data. Same boundary. |
| 82 | `[r]` | [LeakageDetector 2.0: analyzing data leakage in Jupyter-driven ML pipelines](https://arxiv.org/html/2509.15971) | Productises Yang et al.'s three leakage types (overlap, preprocessing, multi-test) with IDE quick-fixes; explicitly reuses their accuracy rather than re-measuring. Evidence that the three-type split is the de-facto taxonomy. |
| 83 | `[r]` | [MLScent: a tool for anti-pattern detection in ML projects](https://arxiv.org/html/2502.18466v1) | 76 AST detectors (TF 13 / PyTorch 12 / HF 10 / sklearn 9 / pandas 8 / numpy 8 / general 16). Its most-fired detector produces 10,795 instances across 43 projects — cited in §9 as the cautionary case that detector count is not coverage. |
| 84 | `[r]` | [AI-Specific Code Smells: From Specification to Detection (SpecDetect4AI)](https://arxiv.org/html/2509.20491v1) | 22 AI smells across 826 systems / 20.5M LOC; reported precision 88.66%, recall 88.89%. Also names its two worst rules — Missing Mask of Invalid Value (F1 0.47) and Threshold-Dependent Validation (F1 0.34) — which is the warning attached to **C-30**. |
| 85 | `[!]` | [An abstract interpretation-based data leakage static analysis (Drobnjaković, Subotić, Urban)](https://arxiv.org/abs/2211.16073) | Partition-membership abstraction (Train/Test/Full/Unknown) propagated through dataflow; reported 93% precision over 2,111 Kaggle notebooks in NBLyzer. The formal statement of what MLView's split tags approximate. **Limit:** abstract-level only; the precision figure is quoted as the authors' own and was not independently checked. |

## G. Peer-reviewed literature on ML defects, smells and leakage

| # | mark | Source | Used for |
|---|------|--------|----------|
| 86 | `[r]` | [Taxonomy of Real Faults in Deep Learning Systems (Humbatova et al., ICSE 2020)](https://arxiv.org/abs/1910.11015) | 5 categories / 92 leaf fault types / 375 instances from 1,059 GitHub and Stack Overflow artifacts plus 20 interviews, validated by a 21-developer survey. "Missing preprocessing" is a top-5 leaf (11 GitHub/SO + 22 interview instances). Grounds §4's severity ordering and **C-25**. |
| 87 | `[r]` | [A Comprehensive Study on Deep Learning Bug Characteristics (Islam et al., ESEC/FSE 2019)](https://arxiv.org/abs/1906.01388) | 2,716 SO posts + 500 fix commits. Root causes: incorrect model parameter/structure 24%, structural inefficiency 19%, absence of type checking 13%, unaligned tensor 11%, API misuse 11%. Pipeline stage: data preparation 32%, training 27%. The stage weighting in §4. |
| 88 | `[r]` | [An Empirical Study on TensorFlow Program Bugs (Zhang et al., ISSTA 2018)](https://xiongyingfei.github.io/papers/ISSTA18b.pdf) | 175 TF bugs; the authors' own note that type confusion and dimension mismatch are the static-analysis-amenable classes. The boundary argument for MLView deliberately having no shape lattice (§9.11). |
| 89 | `[r]` | [Bugs in Machine Learning-based Systems: a faultload benchmark (defect4ML, Morovati et al.)](https://arxiv.org/abs/2206.12311) | 100 reproducible TF/Keras bugs across 30 Humbatova fault types. A ready external recall benchmark, proposed in §7. |
| 90 | `[r]` | [Silent Bugs in Deep Learning Frameworks (Tambon et al.)](https://arxiv.org/abs/2112.13314) | 77 reproducible silent bugs out of 1,168 issues, surveyed with 103 developers: bugs that "lead to wrong behavior but they do not cause system crashes or hangs, nor show an error message". 13% are wrong save/reload. The category argument for severity throughout §5. |
| 91 | `[r]` | [The Prevalence of Code Smells in Machine Learning Projects (van Oort et al.)](https://arxiv.org/abs/2103.04146) | 74 open-source ML projects analysed with Pylint; the explicit finding that Pylint "cannot reliably check correct usage of imported dependencies, including PyTorch". The gap MLView's FQN-resolved tables fill. **Limit:** the full text did not parse; cited at abstract level only. |
| 92 | `[r]` | [Code Smells in Machine Learning Systems (Gesi et al.)](https://arxiv.org/abs/2203.00803) | 5 DL-specific smells with per-project prevalence (Scattered Use of ML Library 54%, Unwanted Debugging Code 41%, Deep God File 37%). Architectural rather than defect-level; cited in §9.12 as out of scope. |
| 93 | `[r]` | [Automatic Fault Detection for Deep Learning Programs Using Graph Transformations (NeuraLint, Nikanjam et al., TOSEM)](https://arxiv.org/html/2105.08095v2) | The closest prior art: a meta-model plus 23 graph-transformation rules, reported 100% precision / 70.5% recall over 34 real programs. Its initialization and architecture rules are the coverage gap named in §4; two of them are rejected on measurement in §9.2. |
| 94 | `[r]` | [Testing Feedforward Neural Networks Training Programs (TheDeepChecker, Braiek & Khomh, TOSEM)](https://arxiv.org/html/2204.00694v1) | A 19-item catalog of training-program issues with pre-/on-/post-fitting routines, and an explicit statement of which subset is statically checkable. §4. |
| 95 | `[r]` | [Leakage and the reproducibility crisis in ML-based science (Kapoor & Narayanan, Patterns 2023)](https://arxiv.org/abs/2207.07048) | The eight-type leakage taxonomy (L1.1 no test set, L1.2 preprocessing on train+test, L1.3 feature selection, L1.4 duplicates, L2 illegitimate features, L3.1 temporal, L3.2 non-independence, L3.3 sampling bias); 294 affected papers across 17 fields. The coverage map in §4 and the severity justification across §5. |
| 96 | `[v]` | [Data Leakage in Notebooks: Static Detection and Better Processes (Yang et al., ASE 2022)](https://arxiv.org/abs/2209.03345) | Datalog over Python AST with map/reduce/duplicate edges, distinguishing validation (repeated use) from test (single use). Abstract verified: over 100,000 public notebooks evaluated, "such leakage is pervasive". **Limit:** the per-type prevalence breakdown (preprocessing / overlap / multi-test) and the 97.6% precision / 67.8% recall figures are in the full text, not the abstract; where this document cites them it says so. |
| 97 | `[r]` | [Hidden Technical Debt in Machine Learning Systems (Sculley et al., NeurIPS 2015)](https://papers.nips.cc/paper/2015/hash/86df7dcfd896fcaf2674f757a2463eba-Abstract.html) | Pipeline jungles, glue code, dead experimental code paths, configuration debt. The structural argument for the train/serve-skew family (§5, tier 3) and the scope boundary in §9.12. |
| 98 | `[r]` | [Data Smells (Foidl, Felderer, Ramler)](https://arxiv.org/abs/2203.10384) | 36 data smells detected by profiling over 240+ datasets. Confirms these are properties of data files, not source — the boundary in §9.10. **Limit:** the full smell list did not parse; cited at the level of the three-group structure only. |
| 99 | `[!]` | [Code Smells for Machine Learning Applications (Zhang, Cruz, van Deursen, CAIN 2022)](https://arxiv.org/abs/2203.13746) | The peer-reviewed source of the 22-smell catalogue that dslinter implements, with a 14 "Code Smell" / 6 "Code Smell Advice" split validated by practitioners. **Limit:** the PDF did not parse in either research pass; individual smell definitions are taken from [75], which is the same catalogue by the same group, and are attributed there. |
| 100 | `[r]` | [On the cross-validation bias due to unsupervised pre-processing (Moscovich & Rosset)](https://arxiv.org/abs/1901.08974) | Even *unsupervised* preprocessing before CV "can… introduce a substantial bias into cross-validation estimates", of unpredictable sign. Why MLV103 is a defect and not a purist's complaint — and why the widening in §6.5 needs no supervised-transformer carve-out. |
| 101 | `[r]` | [Leakage in Data Mining: Formulation, Detection, and Avoidance (Kaufman, Rosset & Perlich, KDD 2011)](https://dl.acm.org/doi/10.1145/2382577.2382579) | The original formulation of leakage as a legitimacy violation and the "no time machine" criterion. Grounds **C-21**, **C-22** and **C-23**. |
| 102 | `[r]` | [A Critical Study on Data Leakage in Recommender System Offline Evaluation (Ji et al., ACM TOIS 2023)](https://dl.acm.org/doi/fullHtml/10.1145/3569930) | Ignoring the global timeline in a recommender split changes accuracy and makes relative model orderings unpredictable across four datasets. Grounds the tier-3 recsys candidate. |
| 103 | `[r]` | [The reusable holdout: preserving validity in adaptive data analysis (Dwork et al., Science 2015)](https://www.science.org/doi/10.1126/science.aaa9375) | Formalises why repeated adaptive access to a holdout degrades its validity. The statistical warrant for **C-02** and **C-30**. |
| 104 | `[r]` | [Rules of Machine Learning (Google / Zinkevich)](https://developers.google.com/machine-learning/guides/rules-of-ml) | The three causes of training-serving skew; Rule #29 (log serving features); Rule #32 "reuse code between your training pipeline and your serving pipeline… This eliminates a source of training-serving skew". Grounds **C-31** (and **C-25**). |
| 105 | `[r]` | [Decoupled Weight Decay Regularization (Loshchilov & Hutter, ICLR 2019)](https://arxiv.org/abs/1711.05101) | L2 regularisation in Adam is not equivalent to weight decay and generalises worse. The severity basis for the Adam-vs-AdamW advisory rejected in §9.4. |
| 106 | `[r]` | [Understanding the Disharmony between Dropout and Batch Normalization by Variance Shift (Li et al., CVPR 2019)](https://arxiv.org/abs/1801.05134) | The mechanism behind NeuraLint's representative-statistics rule. Cited in §9.2 as a rule **not** worth writing: the bad ordering has zero occurrences in the 37-repo corpus. |
| 107 | `[r]` | [Forecasting: Principles and Practice (3rd ed.) §5.10 — Hyndman & Athanasopoulos](https://otexts.com/fpp3/tscv.html) | Evaluation on a rolling forecasting origin: "no future observations can be used in constructing the forecast"; error grows with horizon. Grounds **C-22** and the forecasting sibling under **C-30**. |
| 108 | `[!]` | [Purged cross-validation](https://en.wikipedia.org/wiki/Purged_cross-validation) | Purging and embargoing for overlapping labels. **Secondary source.** The primary is López de Prado, *Advances in Financial Machine Learning* (2018), which could not be retrieved; used only to motivate a `gap=` check, never to assert a numeric embargo length. |
| 109 | `[v]` | [A Grammar of Machine Learning Workflows: Rejecting Data Leakage at Call Time (Roth, 2026)](https://arxiv.org/abs/2603.10742) | Verified: a grammar of eight typed primitives over a DAG with four hard constraints, plus a terminal assessment gate. Abstract states "Data leakage has been identified in 648 published papers across 30 scientific fields" and that "A companion landscape study across 2,047 datasets grounds the constraints in measured effect sizes." **Limit:** the per-class effect sizes (Cohen's *dz*, ΔAUC) are **not** in the abstract and were not verified; §5 therefore cites this paper only for the existence of a measured effect-size ordering, and does not quote any number from it. |

## H. Local sources — the MLView tree itself

| # | mark | Source | Used for |
|---|------|--------|----------|
| 110 | `[v]` | `docs/ISSUE_RULES.md` (this repo) | §1 catalog of 66 codes; §3 the 36 shipped rules with detection and false-positive notes; §4 the 50 later-tier sketches; §5 the twelve-box authoring checklist. Every candidate in §5 states its overlap against this file so nothing is re-derived. |
| 111 | `[v]` | `docs/ACCURACY.md` (this repo) | The corpus definition, the label format, and §9's named recall blockers — several of which this document sizes for the first time. |
| 112 | `[v]` | `docs/CONTRACTS.md` v1.1 (this repo) | The IR, the rule API, the confidence model with evidence weights and buckets, and the five iron laws every detection sketch in §5 is constrained by. |
| 113 | `[v]` | `analyzer/src/mlview/knowledge/gbm_tbl.py` lines 109–118 (this repo) | The PUB2-08 comment: "the native `Booster.predict` returns the **margin or probability**, not a class… Tagging it PREDS made MLV306 fire at 0.90 on `float(roc_auc_score(test_y, booster.predict(dtest)))`, which is the textbook-correct way to score a booster." The precedent for the Keras fix in §6.5 — the same reasoning, never applied to the Keras row. |
| 114 | `[v]` | `analyzer/src/mlview/knowledge/other_tbl.py` line 45 (this repo) | `"keras.Model.predict": E("predict", "eval", K, "PREDICT", ("PREDS",))` — the single row behind the confirmed MLV306 false positive and the paired MLV305 false negative. |
| 115 | `[v]` | `analyzer/src/mlview/rules/holdout_splits.py` lines 63–77 (this repo) | `ORDERED_SPLITTERS` and `_SHUFFLE_OPTIONAL_SPLITTERS`. `GroupShuffleSplit` is in the shuffle-optional set (it has no `shuffle` parameter — see [22]) and `StratifiedGroupKFold` is in neither (its `shuffle` defaults to False — see [23]). |
| 116 | `[v]` | `analyzer/src/mlview/rules/r_mechanics.py` lines 485–503 (this repo) | MLV803's `torch.load` arm: `if "map_location" in call.kwarg_nodes or "weights_only" in call.kwarg_nodes: continue`. An explicit `weights_only=False` is therefore skipped, and the bare call is accused of unpickling arbitrary objects — which [1] shows is false on current PyTorch. |
| 117 | `[v]` | `analyzer/src/mlview/rules/helpers.py` lines 349–362 (this repo) | `is_seeded()` returns True for **any** `random_state=` / `generator=` / `seed=` / `random_seed=` keyword anywhere in the workspace. Correct for MLV601's "is anything seeded at all", and the loophole behind four measured misses (§6.5 D4, **C-07**). |
| 118 | `[v]` | `analyzer/src/mlview/rules/eval_regions.py` lines 27–28 (this repo) | `_EVAL_NAME_RE` matches only `validate`, `validation`, `evaluate`, `evaluation`, `val`, `test`, `testing`, `predict`, `prediction`, `inference`, `infer` and `score` — the name set that a diffusion sampler, a VAE `sample()`, a GAN `sample_grid()` and a retrieval `encode_texts()` all fail to match. §6.3. |
| 119 | `[v]` | `analyzer/src/mlview/rules/valuetype.py` lines 76–101 (this repo) | `_PASSTHROUGH_ROLES` and `_PASSTHROUGH_METHODS = {detach, cpu, cuda, numpy, tolist, clone, contiguous, …}` — the transparent tail that stops short of subscripts and shape ops. §6.1. |
| 120 | `[v]` | `analyzer/tests/accuracy/corpus/*/labels.json` (this repo, 158 files) | 555 `expected` rows, 2,327 `forbidden` rows, 142 `unsupported` rows and 48 `gaps` rows. The 173 rows recording a real defect **no rule can express** are the single best rule-gap oracle in the project and drive most of §5. |
