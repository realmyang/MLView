# MLV711 - Batch-cadence scheduler returned without interval="step"

| | |
|---|---|
| **Severity** | `medium` - amber triangle - likely wrong, or right only under an assumption that cannot be checked statically |
| **Frameworks** | lightning |
| **Base prior** | 0.90 |
| **Rule version** | 1 |
| **Tags** | `correctness`, `framework`, `train-loop` |
| **Absence rule** | no |
| **Enabled** | yes |

## Why it matters

Lightning steps a returned scheduler once per epoch unless it is told otherwise, so a one-cycle schedule completes its whole cycle in the first few epochs and the remaining epochs train at the floor learning rate.

## How it is detected

A `OneCycleLR` / `CyclicLR` / `get_*_schedule_with_warmup` built inside `configure_optimizers` with no `{"interval": "step"}` literal anywhere in that method.

## False positives it avoids

- The correct return shape, `{"scheduler": sched, "interval": "step"}`.
- An epoch-cadence scheduler (`StepLR`, `CosineAnnealingLR`), for which the Lightning default is already right.

## How to fix it

Return {"optimizer": opt, "lr_scheduler": {"scheduler": sched, "interval": "step"}} from configure_optimizers().

## Example that fires

`analyzer/tests/fixtures/rules/MLV711_bad.py`

```python
"""OneCycleLR returned from configure_optimizers with no {"interval": "step"},
so Lightning steps a per-batch schedule once per epoch."""
import pytorch_lightning as pl
import torch
import torch.nn as nn
import torch.nn.functional as F

pl.seed_everything(0)


class OneCycleLit(pl.LightningModule):
    def __init__(self, features: int = 16, classes: int = 3, steps: int = 100) -> None:
        super().__init__()
        self.net = nn.Sequential(nn.Linear(features, 32), nn.ReLU())
        self.head = nn.Linear(32, classes)
        self.steps = steps

    def forward(self, x: torch.Tensor) -> torch.Tensor:
        return self.head(self.net(x))

    def training_step(self, batch, batch_idx):
        features, labels = batch
        return F.cross_entropy(self(features), labels)

    def configure_optimizers(self):
        optimizer = torch.optim.SGD(self.parameters(), lr=1e-2)
        scheduler = torch.optim.lr_scheduler.OneCycleLR(optimizer, max_lr=0.1,
                                                        total_steps=self.steps)
        return {"optimizer": optimizer, "lr_scheduler": {"scheduler": scheduler}}
```

## Example that does not

`analyzer/tests/fixtures/rules/MLV711_good.py` - the nearest false-positive trap this rule has to survive.

```python
"""The trap: the same OneCycleLR in the same return shape, with the one key that
makes it correct - "interval": "step". A rule that only looked for OneCycleLR in
configure_optimizers would fire on this."""
import pytorch_lightning as pl
import torch
import torch.nn as nn
import torch.nn.functional as F

pl.seed_everything(0)


class SteppedLit(pl.LightningModule):
    def __init__(self, features: int = 16, classes: int = 3, steps: int = 100) -> None:
        super().__init__()
        self.net = nn.Sequential(nn.Linear(features, 32), nn.ReLU())
        self.head = nn.Linear(32, classes)
        self.steps = steps

    def forward(self, x: torch.Tensor) -> torch.Tensor:
        return self.head(self.net(x))

    def training_step(self, batch, batch_idx):
        features, labels = batch
        return F.cross_entropy(self(features), labels)

    def configure_optimizers(self):
        optimizer = torch.optim.SGD(self.parameters(), lr=1e-2)
        scheduler = torch.optim.lr_scheduler.OneCycleLR(optimizer, max_lr=0.1,
                                                        total_steps=self.steps)
        return {"optimizer": optimizer,
                "lr_scheduler": {"scheduler": scheduler, "interval": "step"}}
```

## Suppressing it

```python
result = risky_call()  # mlview: ignore[MLV711]
```

Or `# mlview: ignore-file` in the first five lines of the file, or
`.mlview.toml`:

```toml
[rules]
disable = ["MLV711"]
```

A suppressed issue is still emitted with `suppressed: true`, so the viewer can offer "show suppressed"; hosts never publish it as an editor diagnostic.

---

*Generated by `analyzer/tools/gen_rule_docs.py` from the rule registry and the fixtures - do not edit by hand.*
