# dependencies and build output
node_modules/
vscode-extension/out/
*.vsix
*.egg-info/
__pycache__/
*.py[cod]
.pytest_cache/
.mypy_cache/

# Locally fetched, pinned repositories used by native-host evaluations.
.public-corpus/

# Published workflow artifacts and packaged skill ZIPs.
.mlview/
webview/.mlview/

# local orchestration backups and scratch
# C5: `.workflows/` held the multi-agent orchestration scripts of past sprints —
# session artifacts full of absolute machine paths, not part of the product.
.workflows/
*.log
.DS_Store
Thumbs.db

# local virtual environment (macOS setup)
.venv/

# local credentials and environment overrides; keep shareable templates trackable
.env
.env.*
!.env.example
!.env.*.example

# editor and OS scratch that no checkout should carry
.idea/
*.swp
*~

# coverage output (`pytest --cov`, `c8`/`nyc`); no gate reads a committed report
.coverage
.coverage.*
htmlcov/
coverage/
.nyc_output/
